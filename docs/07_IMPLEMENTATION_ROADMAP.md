# 実装ロードマップ

**[← REQUIREMENTS.md に戻る](../REQUIREMENTS.md)**

---

## Phase 1: 環境構築

フロントエンド・バックエンド・データベースの開発環境をセットアップ

**成果物：**
- フロントエンド：Next.js プロジェクト初期化
- バックエンド：Flask/Django プロジェクト初期化
- データベース：MySQL ローカル環境構築
- リポジトリ：Git プロジェクト作成

**チェックリスト：**
- [ ] Node.js 環境構築（v18以上）
- [ ] npm または yarn インストール
- [ ] Next.js プロジェクト作成（`npx create-next-app@latest`）
- [ ] Python 3.9以上 インストール
- [ ] Flask または Django プロジェクト作成
- [ ] MySQL インストール・起動
- [ ] Git リポジトリ初期化
- [ ] ローカル接続確認（フロント 3000, バック 5000 で起動確認）

---

## Phase 2: バックエンド実装（Python）

REST API を実装し、フロントエンドからのリクエストに対応できるようにする

### 2-1: データベーステーブル作成

**成果物：**
- transactions テーブル
- categories テーブル
- 初期データ（10個のカテゴリ）

**チェックリスト：**
- [ ] MySQL `household_db` データベース作成
- [ ] `categories` テーブル作成（ER図に基づく）
- [ ] `transactions` テーブル作成（ER図に基づく）
- [ ] インデックス作成（date, created_at）
- [ ] 初期カテゴリ 10 個をシード（INSERT）
- [ ] データベース接続確認

### 2-2: 取引記録 API 実装

**成果物：**
- `GET /api/transactions?month=YYYY-MM`
- `POST /api/transactions`
- `PUT /api/transactions/:id`
- `DELETE /api/transactions/:id`

**チェックリスト：**
- [ ] `GET /api/transactions` 実装（指定月の記録取得）
- [ ] バリデーション実装（日付、金額、カテゴリID）
- [ ] `POST /api/transactions` 実装（記録保存）
- [ ] `PUT /api/transactions/:id` 実装（記録編集）
- [ ] `DELETE /api/transactions/:id` 実装（記録削除）
- [ ] エラーハンドリング実装
- [ ] 各 API の Postman テスト

### 2-3: カテゴリ API 実装

**成果物：**
- `GET /api/categories`
- `POST /api/categories`
- `DELETE /api/categories/:id`

**チェックリスト：**
- [ ] `GET /api/categories` 実装
- [ ] `POST /api/categories` 実装（新規カテゴリ追加）
- [ ] バリデーション実装（名前の重複チェック、絵文字チェック）
- [ ] `DELETE /api/categories/:id` 実装
- [ ] 初期カテゴリは削除不可の制約実装
- [ ] 各 API の Postman テスト

### 2-4: 集計・グラフ API 実装

**成果物：**
- `GET /api/summary?month=YYYY-MM`
- `GET /api/chart?months=6`

**チェックリスト：**
- [ ] `GET /api/summary` 実装（月別の収入・支出・残高取得）
- [ ] `GET /api/chart` 実装（過去6ヶ月のグラフデータ取得）
- [ ] 集計ロジック実装
- [ ] 各 API の Postman テスト

### 2-5: CORS・セキュリティ設定

**成果物：**
- CORS 設定完了
- 環境変数管理実装
- レート制限実装

**チェックリスト：**
- [ ] Flask-CORS インストール・設定
- [ ] フロントエンドオリジン許可設定
- [ ] `.env` ファイル作成・管理
- [ ] Flask-Limiter インストール・設定（1分100リクエスト）
- [ ] 環境変数の DATABASE_URL 設定

---

## Phase 3: フロントエンド実装（Next.js + TypeScript）

UI コンポーネントを実装し、バックエンド API と連携する

### 3-1: 基本プロジェクト設定

**成果物：**
- TypeScript 設定完了
- API クライアント初期化（axios 等）
- グローバルスタイル設定

**チェックリスト：**
- [ ] TypeScript tsconfig.json 設定
- [ ] axios インストール・設定
- [ ] API ベース URL 設定（環境に応じて切り替え）
- [ ] グローバル CSS 設定
- [ ] レイアウトコンポーネント作成（ヘッダー、フッター）

### 3-2: ホーム画面実装

**成果物：**
- 月別サマリー表示
- 記録一覧表示
- 月選択機能

**チェックリスト：**
- [ ] Home コンポーネント作成
- [ ] `GET /api/summary` 呼び出し実装
- [ ] `GET /api/transactions?month=YYYY-MM` 呼び出し実装
- [ ] サマリー表示（収入・支出・残高）
- [ ] 記録一覧表示（日付降順）
- [ ] 月選択ボタン実装（前月・次月）
- [ ] 記録アイテムのスタイリング
- [ ] 記録クリック → 編集ダイアログ表示
- [ ] ローディング状態表示
- [ ] エラーハンドリング

### 3-3: ダイアログコンポーネント実装

**成果物：**
- 新規記録ダイアログ
- 編集ダイアログ
- 新規カテゴリダイアログ

**チェックリスト：**
- [ ] RecordDialog コンポーネント作成
- [ ] 日付入力フィールド実装
- [ ] 金額入力フィールド実装
- [ ] 種類選択（支出/収入）実装
- [ ] カテゴリセレクト実装（`GET /api/categories` を呼び出し）
- [ ] メモテキスト入力実装
- [ ] クライアント側バリデーション実装
- [ ] 「保存」ボタン実装（`POST /api/transactions`）
- [ ] 「編集」時は `PUT /api/transactions/:id`
- [ ] 「キャンセル」ボタン実装
- [ ] CategoryDialog コンポーネント作成
- [ ] カテゴリ名・アイコン入力実装
- [ ] 「追加」ボタン実装（`POST /api/categories`）

### 3-4: グラフ画面実装

**成果物：**
- 月別収支グラフ表示
- recharts を使用した可視化

**チェックリスト：**
- [ ] Chart.tsx コンポーネント作成
- [ ] recharts インストール
- [ ] `GET /api/chart?months=6` 呼び出し実装
- [ ] 縦棒グラフ実装（収入・支出並表示）
- [ ] X軸：月表示
- [ ] Y軸：金額（円）
- [ ] 凡例：収入（青）・支出（赤）
- [ ] ツールチップ実装
- [ ] 「戻る」ボタン実装
- [ ] ローディング状態表示
- [ ] エラーハンドリング

### 3-5: カテゴリ管理画面実装

**成果物：**
- カテゴリ一覧表示
- カテゴリ削除機能

**チェックリスト：**
- [ ] Categories.tsx コンポーネント作成
- [ ] `GET /api/categories` 呼び出し実装
- [ ] 初期カテゴリ表示（チェックマーク付き）
- [ ] ユーザー作成カテゴリ表示
- [ ] 削除ボタン実装
- [ ] 削除確認ダイアログ実装
- [ ] `DELETE /api/categories/:id` 実装
- [ ] 「新規カテゴリ追加」ボタン → ダイアログ表示
- [ ] 「戻る」ボタン実装

### 3-6: ナビゲーション・レイアウト

**成果物：**
- ハンバーガーメニュー
- ページ遷移機能

**チェックリスト：**
- [ ] ハンバーガーメニュー実装
- [ ] メニュー内のリンク（グラフ、新規記録、カテゴリ管理）
- [ ] Next.js Router.push() で画面遷移
- [ ] URL: `/`, `/chart`, `/categories`
- [ ] 全画面共通ヘッダー実装

### 3-7: スタイリング・UX 改善

**成果物：**
- デザイン完成
- レスポンシブ対応（PC 1024px 以上）
- 操作フィードバック

**チェックリスト：**
- [ ] カラースキーム実装（支出：赤、収入：青）
- [ ] フォントサイズ設定（タイトル 16px、通常 14px）
- [ ] ボタン・フォーム要素のスタイリング
- [ ] トースト通知実装（保存・削除成功時）
- [ ] バリデーションエラーメッセージ表示
- [ ] ホバー・アクティブ状態のスタイル
- [ ] ローディングスピナー表示

---

## Phase 4: ローカル統合テスト

フロント・バック・DB が正常に連携しているかテストする

**成果物：**
- 全機能が正常に動作することを確認

**チェックリスト：**
- [ ] フロントエンド起動（npm run dev）
- [ ] バックエンド起動（python app.py）
- [ ] データベース起動（MySQL）
- [ ] ホーム画面表示確認
- [ ] 新規記録追加テスト
- [ ] 記録編集テスト
- [ ] 記録削除テスト
- [ ] カテゴリ追加テスト
- [ ] カテゴリ削除テスト
- [ ] グラフ画面表示確認
- [ ] 月選択機能確認
- [ ] エラー時の挙動確認（バリデーションエラー等）
- [ ] ブラウザのコンソール確認（エラーなし）
- [ ] ネットワークタブで API 通信確認

---

## Phase 5: AWS デプロイ

本番環境（AWS）にアプリケーションをデプロイ

### 5-1: AWS リソース作成

**成果物：**
- EC2 インスタンス作成
- RDS (MySQL) インスタンス作成
- S3 バケット作成

**チェックリスト：**
- [ ] AWS アカウント作成・セットアップ
- [ ] EC2 インスタンス起動（Ubuntu 22.04 LTS）
- [ ] RDS MySQL インスタンス作成（8.0以上）
- [ ] S3 バケット作成（フロントエンド配信用）
- [ ] Security Group 設定（ポート 80, 443, 3306）
- [ ] Elastic IP 割り当て

### 5-2: バックエンド EC2 デプロイ

**成果物：**
- EC2 上でバックエンド API が稼働

**チェックリスト：**
- [ ] EC2 に SSH ログイン
- [ ] Python 3.9 インストール
- [ ] Git クローン
- [ ] requirements.txt から依存ライブラリインストール
- [ ] 環境変数設定（.env）
- [ ] Gunicorn インストール
- [ ] Nginx インストール・設定
- [ ] HTTPS 設定（SSL 証明書取得）
- [ ] バックエンド起動確認

### 5-3: フロントエンド S3 + CloudFront デプロイ

**成果物：**
- フロントエンドが CloudFront 経由で配信される

**チェックリスト：**
- [ ] Next.js ビルド（npm run build）
- [ ] S3 バケットにアップロード
- [ ] CloudFront ディストリビューション作成
- [ ] キャッシュ設定
- [ ] 独自ドメイン設定（Route 53）
- [ ] HTTPS 設定

### 5-4: 本番確認テスト

**成果物：**
- 本番環境で全機能が動作確認済み

**チェックリスト：**
- [ ] 本番環境にアクセス
- [ ] ホーム画面表示確認
- [ ] 新規記録追加テスト
- [ ] グラフ表示確認
- [ ] カテゴリ管理確認
- [ ] エラー時の挙動確認
- [ ] ログ確認（CloudWatch）

---

## タイムライン目安

| Phase | 期間 | 備考 |
|-------|------|------|
| Phase 1 | 1日 | 環境構築のみ |
| Phase 2 | 3～4日 | バックエンド 実装 |
| Phase 3 | 5～7日 | フロントエンド 実装（最も時間がかかる） |
| Phase 4 | 1～2日 | 統合テスト・バグ修正 |
| Phase 5 | 2～3日 | デプロイ |
| **合計** | **約 2～3週間** | 平均的な見積もり |

---

**[← REQUIREMENTS.md に戻る](../REQUIREMENTS.md)**
